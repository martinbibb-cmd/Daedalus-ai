{
  "name": "daedalus-llm-gateway",
  "version": "1.0.0",
  "description": "Fastify API gateway for Daedalus LLM provider access.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "deploy": "wrangler deploy --config ./wrangler.toml",
    "worker:dev": "wrangler dev --config ./wrangler.toml"
  },
  "keywords": [
    "fastify",
    "gemini",
    "ollama",
    "llm",
    "gateway"
  ],
  "author": "",
  "license": "UNLICENSED",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "wrangler": "4.105.0"
  }
}
